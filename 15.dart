import 'dart:io';
import 'dart:convert';

void main() {
  //int? ano = int.parse(stdin.readLineSync()!);
  //var tratamento = [ano];
  //print(tratamento);
  // var minhaLista = [];

  // for (var i = 0; i < 1; i++) {
  //   var minhaLista = [stdin.readLineSync()];
  // }
  // print(minhaLista);

  // var arr = new List(4);
  // arr[0] = '1';
}
